name: ANEWON Restaurant City branch verification

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  delegated:
    runs-on: ubuntu-24.04
    if: ${{ false }}
    steps:
      - run: echo "Physical verification is SHA-pinned and executed by anewonhq/anew-revival-lab on ANEWON-REVIVAL-01."
