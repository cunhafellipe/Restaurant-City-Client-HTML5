[package]
name = "restaurant-city-authority"
version = "0.1.0"
edition = "2024"
publish = false
rust-version = "1.98.1"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
redb = "=4.3.0"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
